apiVersion: apps/v1
kind: Deployment
metadata:
  name: brave-dog-deployment
  labels:
    app: brave-dog-deployment-label
spec:
  replicas: 1
  selector:
    matchLabels:
      app: brave-dog-label
  template:
    metadata:
      labels:
        app: brave-dog-label
    spec:
      containers:
        - name: brave-container
          image: meloyd/brave
          ports:
            - name: api
              containerPort: 5000
            - name: output
              containerPort: 7000
